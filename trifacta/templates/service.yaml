apiVersion: v1
kind: Service
metadata:
  name: {{ template "trifacta.fullname" . }}
  labels:
    app: {{ template "trifacta.name" . }}
    chart: {{ template "trifacta.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "trifacta.name" . }}
    release: {{ .Release.Name }}
